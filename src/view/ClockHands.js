import styled from 'styled-components';

export const ClockHands = styled.p`
  font-family: 'Carlito', sans-serif;
  white-space: nowrap;
  position: absolute;
  line-height: 220px;
  top: -110px;
  transition: all 0.5s ease-out;
  transform-origin: left center 0;
`;
